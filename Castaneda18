import javax.swing.*;

public class Castaneda18 {
    public static void main(String[] args) {
        int[] arr = {5, 3, 6, 4, 2, 9};
        boolean dup = false;

        for (int i = 0; i < arr.length; i++) {
            for (int j = i + 1; j < arr.length; j++) {
                if (arr[i] == arr[j]) {
                    dup = true;
                    break;
                }
            }
        }

        JOptionPane.showMessageDialog(null, "Contains duplicates? " + dup);
    }
}
